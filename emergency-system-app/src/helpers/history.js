import { createBrowserHistory } from "history";

// history object used by React Router that support the HTML5 history API.
// It provides a minimal API that lets you manage the history stack, navigate,
// and persist state between sessions
export const history = createBrowserHistory();
